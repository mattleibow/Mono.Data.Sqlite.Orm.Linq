
To build the SQLite provider you will need to have SQLite installed.  

You can get SQLite and the .Net data provider from http://system.data.sqlite.org

After you've installed SQLite, you may need to re-add the reference to the System.Data.SQLite.dll depending on where it is installed.


